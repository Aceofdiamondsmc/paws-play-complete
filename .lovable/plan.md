

## Revert SOS FAB Position

One-line change in `src/components/lost-dog/LostDogFAB.tsx` (line 17):

Change `bottom-36` back to `bottom-28`. Avatars scroll with comments so no overlap issue exists.

